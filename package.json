{
  "name": "cicd-pipeline-demo",
  "version": "1.0.0",
  "description": "Sample Node.js/Express app demonstrating a production-ready CI/CD pipeline with GitHub Actions",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "test": "jest --coverage",
    "build": "echo 'No transpile step needed for plain Node.js — build check passed' && node -c src/index.js"
  },
  "keywords": ["cicd", "github-actions", "devops", "express"],
  "author": "Aryan",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
